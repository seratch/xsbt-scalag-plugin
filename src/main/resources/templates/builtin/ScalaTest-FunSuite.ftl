<#if packageName != "">package ${packageName}</#if>

import org.scalatest._
import org.scalatest.matchers._

class ${name}Suite extends FunSuite with ShouldMatchers {

  test("available") {
    pending
  }

}
